using UnityEngine;

public class HazardPheromone : Pheromone
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        pheromoneType = PheromoneType.Hazard;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
